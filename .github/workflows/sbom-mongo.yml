# SBOM workflow for MongoDB (Docker Official)
#
# Triggers when mongo config is updated.
# Extracts SBOM from Docker OCI attestation (SPDX).
# Also picked up by tea-sync hourly for image digest changes.
#
# https://hub.docker.com/_/mongo

name: "SBOM: mongo"

on:
  push:
    branches:
      - master
    paths:
      - 'apps/mongo/config.yaml'
      - '.github/workflows/sbom-mongo.yml'

  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Run in dry-run mode (no upload)'
        required: false
        type: boolean
        default: false

jobs:
  build:
    uses: ./.github/workflows/sbom-builder.yml
    with:
      app: mongo
      dry_run: ${{ github.event.inputs.dry_run == 'true' }}
    secrets: inherit
    permissions:
      id-token: write
      contents: read
      attestations: write
