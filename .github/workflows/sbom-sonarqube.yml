# SBOM workflow for SonarQube (Docker Official)
#
# Triggers when sonarqube config is updated.
# Extracts SBOM from Docker OCI attestation (SPDX).
# Also picked up by tea-sync hourly for image digest changes.
#
# https://hub.docker.com/_/sonarqube

name: "SBOM: sonarqube"

on:
  push:
    branches:
      - master
    paths:
      - 'apps/sonarqube/config.yaml'
      - '.github/workflows/sbom-sonarqube.yml'

  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Run in dry-run mode (no upload)'
        required: false
        type: boolean
        default: false

jobs:
  build:
    uses: ./.github/workflows/sbom-builder.yml
    with:
      app: sonarqube
      dry_run: ${{ github.event.inputs.dry_run == 'true' }}
    secrets: inherit
    permissions:
      id-token: write
      contents: read
      attestations: write
