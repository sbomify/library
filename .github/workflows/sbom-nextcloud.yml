# SBOM workflow for Nextcloud (Docker Official)
#
# Triggers when nextcloud config is updated.
# Extracts SBOM from Docker OCI attestation (SPDX).
# Also picked up by tea-sync hourly for image digest changes.
#
# https://hub.docker.com/_/nextcloud

name: "SBOM: nextcloud"

on:
  push:
    branches:
      - master
    paths:
      - 'apps/nextcloud/config.yaml'
      - '.github/workflows/sbom-nextcloud.yml'

  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Run in dry-run mode (no upload)'
        required: false
        type: boolean
        default: false

jobs:
  build:
    uses: ./.github/workflows/sbom-builder.yml
    with:
      app: nextcloud
      dry_run: ${{ github.event.inputs.dry_run == 'true' }}
    secrets: inherit
    permissions:
      id-token: write
      contents: read
      attestations: write
